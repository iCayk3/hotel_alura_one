ANTES DE UTILIZAR A APLICAÇÃO CRIAR UM BANCO DE DADOS E RODAR O SCRIPT SQL DISPONIBILIZADO NESSE README;

###SCRIPT SQL###

CREATE DATABASE IF NOT EXISTS HOTEL_ALURA;

USE HOTEL_ALURA; 

CREATE TABLE IF NOT EXISTS 
	RESERVA(ID INT AUTO_INCREMENT PRIMARY KEY, 
	DATA_ENTRADA DATE NOT NULL, 
	DATA_SAIDA DATE NOT NULL, 
	VALOR DECIMAL(6,2) NOT NULL, 
	FORMA_PAGAMENTO VARCHAR(30) NOT NULL);
	
CREATE TABLE IF NOT EXISTS 
	HOSPEDE(ID INT AUTO_INCREMENT PRIMARY KEY, 
	NOME VARCHAR(50) NOT NULL, 
	SOBRENOME VARCHAR(50),
	DATA_NASCIMENTO DATE NOT NULL, 
	NACIONALIDADE VARCHAR(30), 
	TELEFONE VARCHAR(20), 
	ID_RESERVA INT);

ALTER TABLE HOSPEDE ADD FOREIGN KEY(ID_RESERVA) REFERENCES RESERVA (ID);